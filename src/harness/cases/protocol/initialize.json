{
  "version": 1,
  "id": "protocol.initialize",
  "kind": "protocol",
  "title": "Initialize",
  "description": "验证 agent 是否能完成 ACP initialize 握手并返回能力信息。",
  "protocolDependencies": ["initialize"],
  "steps": [
    { "type": "initialize" }
  ],
  "assertions": [
    { "type": "transcript-has-method", "method": "initialize" }
  ]
}
