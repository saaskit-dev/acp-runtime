{
  "version": 1,
  "id": "protocol.available-commands-update",
  "kind": "protocol",
  "title": "Available Commands Update",
  "description": "验证 agent 是否通过 session/update 广播可用 slash commands。",
  "protocolDependencies": ["initialize", "session/new", "session/update"],
  "steps": [
    { "type": "initialize" },
    { "type": "session-new" }
  ],
  "assertions": [
    { "type": "transcript-has-event", "eventType": "available_commands_update" }
  ]
}
